<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="net.idt.trunkmon.LoginActivity"
    tools:showIn="@layout/activity_login"
    android:background="#daf2ecec">

    <com.beardedhen.androidbootstrap.BootstrapEditText
        android:layout_width="fill_parent"
        android:layout_height="35dp"
        app:bootstrapSize="md"
        app:bootstrapBrand="primary"
        android:id="@+id/et_boot_username"
        android:hint="Username"
        android:inputType="textAutoComplete|textAutoCorrect"
        android:layout_above="@+id/et_boot_password"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="30dp" />

    <com.beardedhen.androidbootstrap.BootstrapEditText
        android:layout_width="fill_parent"
        android:layout_height="35dp"
        android:id="@+id/et_boot_password"
        android:layout_marginBottom="69dp"
        app:bootstrapBrand="primary"
        android:layout_above="@+id/boot_btn_login"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:hint="Password">
    </com.beardedhen.androidbootstrap.BootstrapEditText>

    <com.beardedhen.androidbootstrap.BootstrapButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Login"
        app:bootstrapBrand="primary"
        app:bootstrapSize="lg"
        app:buttonMode="regular"
        app:showOutline="false"
        app:roundedCorners="true"
        android:id="@+id/boot_btn_login"
        android:focusableInTouchMode="false"
        android:layout_marginBottom="164dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
